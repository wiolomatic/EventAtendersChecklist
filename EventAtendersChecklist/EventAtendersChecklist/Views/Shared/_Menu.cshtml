@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    <ul class="nav navbar-nav">
        @if (User.IsInRole("HR"))
        {
        <li>@Html.ActionLink("Events", "Index", "Events")</li>
        <li>@Html.ActionLink("History", "History", "Events")</li>
        <li>@Html.ActionLink("Attenders", "Index", "Employees")</li>
        <li>@Html.ActionLink("Actions", "Index", "ActionDictionaries")</li>
        }
        else
        {
            <li>@Html.ActionLink("Events", "Index", "Events")</li>
        }
    </ul>
    @Html.Partial("_LoginPartial")
}
else
{
    @Html.Partial("_LoginPartial")
}
